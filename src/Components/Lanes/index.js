import React from "react";

const Lane = ({ title }) => {
  return (
    <section>
      <h1>{title}</h1>
      <button>
        <ion-icon name="add-outline"></ion-icon>
      </button>
      CARD
    </section>
  );
};

export default Lane;
